import { HighlightButtonsDirective } from './highlight-buttons.directive';

describe('HighlightButtonsDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightButtonsDirective();
    expect(directive).toBeTruthy();
  });
});
