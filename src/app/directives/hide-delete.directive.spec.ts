import { HideDeleteDirective } from './hide-delete.directive';

describe('HideDeleteDirective', () => {
  it('should create an instance', () => {
    const directive = new HideDeleteDirective();
    expect(directive).toBeTruthy();
  });
});
